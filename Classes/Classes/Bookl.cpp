#include <iostream>
using namespace std;
#include <string>
/*
//접근 제어자
class Book {
private:
	int number;			//번호
	string title;		//제목
	string author;		//저자
public:
	Book(){} // 기본 생성자 - 생략가능(자동으로 추가되기 때문에
	//책 번호 입력 함수
	void setNumber(int num) //private에 접근하기 위한 멤버 함수 생성 - set + 변수() get + 변수()
	{
		number = num;
	}

	//책 번호 가져오는 함수
	int getNumber()
	{
		return number;
	}


	//책 제목 입력하고
	void setTitle(string tit) {
		title = tit;
	}
	//책 제목 가져오기
	string getTitle()
	{
		return title;


	}
	void setAuthor(string aut) {
		author = aut;
	}
	//책 제목 가져오기
	string getAuthor()
	{
		return author;


	}
};




int main_ooo()
{
	Book book1;

	book1.setNumber(100); // 기본이 private이므로 접근 불가
	cout << "책 번호 : " << book1.getNumber() << endl;
	book1.setTitle("육식"); // 기본이 private이므로 접근 불가
	cout << "책 제목 : " << book1.getTitle() << endl;
	book1.setAuthor("띠리리"); // 기본이 private이므로 접근 불가
	cout << "작가 이름 : " << book1.getAuthor() << endl;

	return 0;

	*/
